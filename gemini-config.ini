[DEFAULT]

[gemini/gemini-powerpc-legacy-bsps]
rpm_name = rtems
rpm_revision = %{gemini_version}.%{rsb_revision}%{?dist}

[gemini/gemini-powerpc-net-legacy-bsps]
rpm_name = rtems
rpm_revision = %{gemini_version}.%{rsb_revision}%{?dist}
